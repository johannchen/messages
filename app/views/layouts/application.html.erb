<!DOCTYPE html>
<html ng-app>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Sermon Messages" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#">Sermon Messages</a>
          <div class="container nav-collapse">
            <ul class="nav">
              <% if current_user %>
                <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  Messages<b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Summary View", messages_path %></li>
                  <li><%= link_to "Calendar View", messages_path(:view => "calendar") %></li>
                  <li><%= link_to "List View", messages_path(:view => "list") %></li>
                  <li><%= link_to "Add Message", new_message_path %></li>
                  <li><%= link_to "Download CSV", messages_path(:format => :csv) %></li>
                  </ul>
                </li>
                <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  Verses<b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <li><%= link_to "View Favorites", verses_path %></li>
                  <li><%= link_to "View All", verses_path(view: "all") %></li>
                  <li><%= link_to "Add Verse", new_verse_path %></li>
                  </ul>
                </li>
<%= active_link_to 'Speakers', speakers_path, wrap_tag: :li %>
                <%= active_link_to 'Categories', categories_path, wrap_tag: :li %>
                <%= active_link_to 'Feedback', feedback_path, wrap_tag: :li %>
                <%= active_link_to 'About', root_path, wrap_tag: :li %>
                <% if mobile_device? %>
                  <li><%= link_to 'Full Site', mobile: 0 %></li>
                <% else %>
                  <li><%= link_to 'Mobile', root_path(mobile: 1) %></li>
                <% end %>
                <li><%= link_to 'Sign Out', signout_path %></li>
                <% if current_user.admin? %>
                  [<%= User.count %>]
                <% end %>
              <% else %>
                <li><%= link_to 'Sign in with Google', '/auth/google' %></li>
                <li><%= link_to 'Sign in with Facebook', '/auth/facebook' %></li>
              <% end %>
            </ul>
            <% if current_user %>
              <%= form_tag messages_path, method:'get', class: 'navbar-search pull-right' do %>
                <%= text_field_tag :search, params[:search], class: 'span2' %>
              <% end %>
            <% end %>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <div class="row">
          <%= yield %>
        </div><!--/row-->
      </div><!--/content-->

      <footer>
        <p>&copy; Company 2012</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
